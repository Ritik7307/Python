## Day-1
Python is a dynamically typed, general purpoe programming language that supports an object oriented programming approach as well as a functional programming approach.
Its a high level programming language 
Pythin is a open-source programming language
It provides ery big libraries support , NumPy,Tensorflow,Selenium,Opencv

## What is Python used for?

 Python is used for Data Visualisation to create plots and graphical representation
 It helps in Data Analytics to analyze and understand raw data for insights and trends
 To create web applications
 Used to handle databases
 Business and Accounnting to perform complex maths operation alongwith quantitative and qualitative analysis.

## Day-3 : Modules and pip in Python

Module is like a code library which can be used to borrow the code written by somebody else in ou python program .There are two types of modules in python:

 ->Built in Module - These modules are to import and also ships with python interpreter .there is no need to install such modues explicitly.

 ->External Modules - These modules are imported from the third party file or can be installed using a package manager like pip or conda. Since this code is written by someone else, we can install different versions pf a same module with time.

## The pip command
It can be used as a package manager pip to install a python modue.
## Example - pip install pandas // Data analysis library

## Variables and Datatypes


## Type Casting in Python
The conversion of one data type into the other data type is known as type Casting in python or type conversion in python
Python supports a wide variety of functions or methods like : int(), float(), str(), ord(), hex(), oct(), tuple(), set(), list(), dict(), etc.
->Explicit Type Casting
->Implicit type Casting